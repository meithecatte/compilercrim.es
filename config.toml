# The URL the site will be built for
base_url = "https://compilercrim.es/"
output_dir = "docs"
title = "The compiler crimes blog"

compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
#build_search_index = true

generate_feed = true
feed_filename = "rss.xml"

[markdown]
highlight_code = true
highlight_theme = "monokaiak"
extra_syntaxes = ["syntaxes"]
extra_highlight_themes = ["themes"]
render_emoji = true
render_with_section_tags = "flat"

[extra]
# Put all your custom variables here
